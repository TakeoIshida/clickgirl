# ClickGirl — ゲーム仕様書

## 概要

女の子キャラクターをタップして売上を稼ぎ、社員を採用・育成しながら会社を大きくしていくクリッカー系放置ゲーム。

- **プラットフォーム**: iOS
- **技術**: Swift / SpriteKit
- **通貨単位**: 円（¥）

---

## シーン構成

| シーン | 役割 |
|--------|------|
| GameScene | メイン画面（タップ・社員パネル・HUD） |
| ShopScene | アップグレード・ブースト購入 |
| OfficeScene | オフィス拡張・設備強化 |
| GachaScene | ガチャ（カード収集） |
| ZukanScene | 図鑑（キャラ画像コレクション） |
| CityScene | 都市開発（建物建設・収益） |

---

## キャラクター（社員）

| ID | 名前 | 役職 | 採用コスト | 基本収益/秒 | 画像枚数 |
|----|------|------|-----------|------------|---------|
| 0 | カレン | 営業部長 | ¥100 | 1.0 | 10枚 |
| 1 | みさき | 開発部長 | ¥1,500 | 15.0 | 8枚 |
| 2 | ゆき | 管理部長 | ¥20,000 | 200.0 | 10枚 |
| 3 | りお | マーケティング部長 | ¥300,000 | 3,000.0 | 3枚（追加予定） |

### 社員レベルアップ

- **収益**: `baseIncomePerSec × level`
- **アップグレードコスト**: `baseCost × 1.15^level`

---

## 画像仕様

### 標準サイズ

- **新規作成サイズ**: **784 × 1176 px**（縦長・3:4.5比率）
- **形式**: PNG（背景なし `_nobg`）または JPG（背景あり）

### ファイル命名規則

| 種別 | 命名パターン | 例 |
|------|------------|-----|
| 背景ありJPG（図鑑用） | `{prefix}_{index}.jpg` | `karen_0.jpg` |
| 背景なしPNG（ゲーム表示用） | `{prefix}_{index}_nobg.png` | `karen_0_nobg.png` |

### 各キャラ画像一覧

| キャラ | prefix | _nobg枚数 | 備考 |
|--------|--------|----------|------|
| カレン | karen | 10枚（0-9） | |
| みさき | misaki | 8枚（0-7） | |
| ゆき | yuki | 10枚（0-9） | |
| りお | rio | 3枚（0-2） | AIで背景除去済み |

### 既存画像の処理（元が16:9横長だった場合）

元の画像が16:9（1368×768等）だった_nobg PNGは、784×441にリサイズして784×1176キャンバスの**縦中央**に配置済み（上下各368px透明余白）。

---

## 画面レイアウト

### GameScene（メイン画面）

#### 縦方向の領域分割

```
┌─────────────────────┐ ← frame.height
│  HUD（120pt）        │   所持金・収益表示、ナビボタン
├─────────────────────┤ ← frame.height - 120
│                     │
│  メインキャラ表示     │   _nobg PNG を使用
│  幅: frame.width×0.85│   HUDと社員パネルの間に縦中央配置
│  高さ: アスペクト比自動│   最大高さ=(エリア高さ×0.95)で自動縮小
│                     │
├─────────────────────┤ ← y = 210
│  オフィスちびキャラ   │   chibiW=36pt（縦長画像対応サイズ）
│  (layerH=120pt)     │
├─────────────────────┤ ← y = 210
│  社員パネル（210pt） │   5キャラのカード表示
└─────────────────────┘ ← y = 0
```

#### HUDNode デザイン

- **背景**: ダーク紺 `(0.05, 0.05, 0.15, 0.92)` + 金枠 `lineWidth=1.5`
- **ゴールドアクセントライン**: HUD下端（y=0）に高さ1.5ptの金色ライン + 薄いグロー層
- **会社名**: "株式会社 ClickGirl"、左上にパルスする ✦ デコ（金色）
- **所持金**: 金色、fontSize=30、左寄せ
- **毎秒収益**: 緑色、fontSize=13、左寄せ

#### HUD ナビボタン（4つ、2列×2行）

| ボタン | ラベル | 色 |
|--------|--------|-----|
| zukanBtn | 📖 図鑑 | 紫系 |
| gachaBtn | 🎰 ガチャ | 濃紫 + 外側パルスグロー（紫、0.9s周期） |
| shopBtn | 🛒 Shop | 緑系 |
| officeBtn | 🏢 Office | 青系 |

- **サイズ**: 72×26pt、cornerRadius=9
- **ガチャボタン**: 外側にパルスするグローリング（78×32pt、紫、0.9秒フェードサイクル）

#### GameScene コンボバナー

- `comboLabelNode` は `SKNode` 型（バナー全体）
- バナー背景（SKShapeNode）+ ドロップシャドウ付きラベルで構成
- コンボ数に応じて色が変化（10以上=金、5以上=オレンジ、それ以外=水色）

#### タップヒント

- タップエリア下部にセミ透明の青ピル背景（210×26pt、cornerRadius=13）
- "タップして売上アップ！" テキスト付き

#### 社員カード画像表示

- 画像: 背景ありJPG（`employee.imageName`）を使用
- 幅: 最大138pt
- 高さ: アスペクト比維持、**最大110pt**（超過時は幅を自動縮小）
- 縦長画像（りお 784×1176）は幅が自動的に縮小される

#### 社員カードのアフォーダブルグロー

- 採用・アップグレードボタンが購入可能になると、緑のパルスグローリングが出現
- 不可の場合はグローを削除（`name: "affordableGlow"` で管理）

#### EmployeePanelNode デコレーション

- パネル最上部にゴールドアクセントライン（height=1pt）
- ヘッダー: ✦（金）+ "社員一覧"（金色テキスト）+ ✦（金）

---

### OfficeScene（オフィス画面）

#### TopBar

- ゴールドアクセントライン（height=1.5pt）+ グロー層（height=8pt）をTopBar下端に配置

#### ブーストバー

- 🏢 アイコンノードを左端に独立配置（x=26）
- テキストは左寄せ（x=42）で "ブースト中: ×N.N | 残 Xs" 形式

#### 都市ナビボタン

- 外側にパルスするグローリング（青、1.0s周期）

#### フロアカード

- アフォーダブル時: ボタン外側に青パルスグローリング（1.0s周期）

#### 設備カード（makeCard）

- **グリーン収益ピル**: 90×17pt（cornerRadius=8）、+¥N/s 表示
- **アフォーダブルグロー**: 購入可能時に外側パルス青グローリング
- **MAXピル**: 背景付き（緑系）

#### ウィンドウ演出（drawWindow）

- Lv2以上: 台形ライトビーム（白半透明）がウィンドウ前に描画 + パルスアニメ

#### トースト（showToast）

- 画面下部配置（y=72→92）
- 下からスライドイン（moveBy y:+20）→ 2秒後にスライドアウト

---

### CityScene（都市開発画面）

#### TopBar

- ゴールドアクセントライン（height=1.5pt）+ グロー層
- 左: ◀ 戻る、中央: 🌆 都市開発、右: 所持金 / 都市収益

#### 都市ビュー（アイソメトリック、215pt高）

タイル定数:
```
tW = 76pt  // タイル幅
tH = 38pt  // タイル高さ
gox = -19  // グリッド横オフセット（3列中央揃え）
goy = -22  // グリッド縦オフセット（下寄せ）
```

座標変換:
```swift
isoCenter(col, row) = (
    x: (col - row) * tW / 2 + gox,
    y: -(col + row) * tH / 2 + goy
)
```

描画レイヤー（奥→手前）:

| Layer | 内容 |
|-------|------|
| 1 | 空（青グラデーション、cornerRadius=12） |
| 2 | 遠景ビル群（暗青シルエット + 窓ランダム点灯） |
| 3 | 水面（台形パス + 白シマーパルス） |
| 4 | 地面（緑）+ 道路ベース（ダークグレー） |
| 5 | アイソメトリックプロット×6（奥行きソート、col+row 昇順） |
| 6 | 木×6本、街灯×2本 |

各プロットの描画:
1. 道路ダイヤモンド（tW+10、tH+5）
2. 歩道ダイヤモンド（tW-2、tH-1）
3. 建物（3面: 左面/右面/屋根）または 空き地（緑タイル + ＋マーク）

建物描画:
- 建物高さ: `buildingHeight(level) × 0.48`（アイソメトリックスケール）
- 左面（暗め）・右面（さらに暗め）・屋根（フル輝度）の3面構成
- 窓: 左面にパラレログラム補間で配置、ランダム点灯
- 屋根アクセント: タワー=アンテナ+赤点滅ライト、ホテル=プール、モール=ストライプ
- 屋根アイコン: 建物タイプの絵文字
- 屋根グロー: 建物色のパルスリング
- Lv2以上: 金色レベルバッジ（"L2" 等）

ビルのアイソメトリック高さ基準値（`buildingHeight(level:)`）:

| 種類 | baseHeight | heightPerLevel |
|------|-----------|----------------|
| カフェ | 38 | 12 |
| レストラン | 55 | 18 |
| ホテル | 85 | 28 |
| モール | 72 | 22 |
| タワー | 130 | 40 |

#### 建物データ

| ID | 名前 | 建設コスト | 基本収益/秒 | 収益倍率 | 最大Lv |
|----|------|----------|-----------|--------|--------|
| cafe | カフェ | ¥50,000 | 50 | ×1.6 | 5 |
| restaurant | レストラン | ¥300,000 | 300 | ×1.7 | 4 |
| hotel | ホテル | ¥2,000,000 | 2,000 | ×1.8 | 4 |
| mall | ショッピングモール | ¥10,000,000 | 10,000 | ×1.9 | 3 |
| tower | タワー | ¥80,000,000 | 80,000 | ×2.0 | 3 |

- アップグレードコスト: `placeCost × upgradeMult^level`
- 収益: `baseIncome × incomeMult^(level-1)`

#### プロット一覧UI（スクロールリスト）

- 6プロット、2列×3行グリッド、スクロール対応（SKCropNodeでクリップ）
- 建設済みカード: 建物アイコン・名前・レベルドット・収益ピル・アップグレードボタン
- 空き地カード: 🏗️・"空き地 #N"・"＋ 建設する"ボタン
- アップグレード可能時: 青パルスグローリング
- MAXレベル到達: ✅ MAX 表示

#### 建設選択オーバーレイ

- 暗転背景 + モーダルパネル（cornerRadius=20）
- 金アクセントライン + 角デコドット
- 建物行: スライドイン登場アニメ（0.06s刻み）、アフォーダブル/ロック色分け
- MAX Lv. バッジを右端に表示
- キャンセルボタン（赤系）

#### エフェクト

- **建設完了バナー**: 中央ポップイン + 金パーティクル8個バースト
- **エラートースト**: 画面下部スライドアップ、2秒後フェードアウト

---

## 収益システム

### タップ収益

```
タップ値 = (1.0 + 採用済み社員の収益/秒の合計 × 0.1) × tapMultiplier
```

### 自動収益（毎秒）

```
totalIncomePerSec = (社員収益合計 × incomeMultiplier × officeIncomeMultiplier × floorIncomeMultiplier) + cityIncomePerSec
```

### コンボシステム

- 0.85秒以内に連続タップするとコンボカウント増加
- コンボボーナス: `+10% × (コンボ数 − 1)` （上限 +100%）
- 0.85秒タップなしでリセット

---

## ショップ

### 永続アップグレード（1回購入）

| ID | 名前 | 効果 | コスト | 前提条件 |
|----|------|------|--------|----------|
| tap_lv1 | タップ強化 Lv.1 | タップ収益 +50% | ¥30,000 | なし |
| tap_lv2 | タップ強化 Lv.2 | タップ収益 さらに+100% | ¥200,000 | tap_lv1 |
| tap_lv3 | タップ強化 Lv.3 | タップ収益 さらに+150% | ガチャSR限定 | tap_lv2 |
| income_lv1 | 収益強化 Lv.1 | 自動収益 +30% | ¥100,000 | なし |
| income_lv2 | 収益強化 Lv.2 | 自動収益 さらに+70% | ¥500,000 | income_lv1 |
| income_lv3 | 収益強化 Lv.3 | 自動収益 さらに+200% | ガチャSSR限定 | income_lv2 |
| auto_tap | 自動タップ | 毎秒 自動で1タップ | ¥80,000 | なし |
| offline_ext | 夜勤延長 | 放置上限 8h → 16h | ¥300,000 | なし |

### 時限ブースト（何度でも購入可）

| ID | 名前 | 効果 | コスト | 時間 |
|----|------|------|--------|------|
| boost_tap2 | タップ 2倍 | タップ収益 ×2 | ¥10,000 | 30秒 |
| boost_income2 | 収益 2倍 | 自動収益 ×2 | ¥50,000 | 60秒 |
| boost_income5 | 収益 5倍 | 自動収益 ×5 | ¥300,000 | 30秒 |
| boost_all3 | 全力 3倍 | 全収益 ×3 | ¥200,000 | 60秒 |

---

## ガチャ

### コスト

| 種別 | コスト |
|------|--------|
| シングル | ¥50,000 |
| 10連 | ¥450,000（1回分お得） |

### レアリティ・排出率

| レアリティ | 排出率 | 収益ボーナス（カード1枚あたり） |
|-----------|--------|-------------------------------|
| N | 60% | +2% |
| R | 30% | +5% |
| SR | 8% | +15% |
| SSR | 2% | +40% |

### 天井

- 10回ごとに **R以上** が確定

### カード演出（めくり）

ガチャ結果はカードの裏面が表示された状態で始まり、プレイヤーが能動的にめくって公開する。

#### 1枚引き
1. カード裏面がポップインで登場
2. ヒント「タップしてめくろう！」が表示される
3. カードをタップ → フリップアニメーションで表面を公開
4. SR/SSR の場合はフラッシュエフェクトが発動
5. ヒントが「タップで閉じる」に切り替わる

#### 10連引き
1. 10枚の裏面が左上から順に波状にポップイン（0.08秒刻み）
2. 「✨ 全てめくる」ボタンと「カードをタップしてめくろう」ヒントが表示される
3. **個別めくり**: 各カードをタップすると1枚ずつフリップ
4. **まとめてめくる**: 「全てめくる」ボタンをタップすると残りを左から順に連続フリップ（0.12秒刻み）
5. 全カードめくり完了後、「全てめくる」ボタンが消えてヒントが「タップで閉じる」に切り替わる

#### カード裏面デザイン
- 深紫背景 + 紫ボーダー、回転する ✨ アイコン、「CLICK GIRL」テキスト（小カードは「CG」）

#### フリップアニメーション仕様
- 前半: 裏面の xScale が 1 → 0（easeIn、0.13秒）
- めくり瞬間: 白いシマー（光）が瞬く
- 後半: 表面の xScale が 0 → 1.08 → 1.0（easeOut + バウンス、0.18秒）

### 10連グリッドレイアウト

**配置: 3+3+3+1**（3列×3行 + 最後の1枚を画面中央）

カードサイズを画面幅・高さから動的に計算し、4行が利用可能領域に収まるよう上限クランプを適用する。

```
cardW = floor((画面幅 - 24 - 10×2) / 3)
cardH = min(floor(cardW × 1.58), floor((利用可能高さ - 8×3) / 4))
```

| 画面幅 | カードサイズ（目安） |
|--------|------------------|
| 390pt（iPhone 14等） | 115 × 160 pt |
| 375pt（iPhone SE等） | 110 × 153 pt |

- 余白: 左右各 12pt、カード間 10pt、行間 8pt
- 縦位置: 「全てめくる」ボタン領域（下115pt）と上部マージン（65pt）の間でグリッド全体を縦中央寄せ
- 最終10枚目は単独で画面横中央に配置
- フォント・バッジ・NEWラベルはカードサイズに比例してスケール

### カード収益倍率

同じキャラのカードを複数枚所持するほど、そのキャラの収益に倍率が加算される。

```
cardIncomeMultiplier = 1.0 + Σ(rarity.incomeBonus × 所持枚数)
```

---

## 図鑑（ズカン）

各キャラクターには複数の衣装・画像が存在し、ガチャカードを集めることで解放される。

| キャラ | 総画像数 | 内訳 |
|--------|---------|------|
| カレン | 10枚 | N×4 / R×3 / SR×2 / SSR×1 |
| みさき | 8枚 | N×3 / R×3 / SR×1 / SSR×1 |
| ゆき | 10枚 | N×4 / R×3 / SR×2 / SSR×1 |
| りお | 3枚（追加予定） | N×3 |

### 解放条件

| レアリティ | 必要枚数 |
|-----------|---------|
| N | 1枚 |
| R | 1枚 |
| SR | 2枚 |
| SSR | 3枚 |

解放した画像はメインキャラとして設定可能。社員パネルのキャラ画像エリアをタップすることで切り替える。

---

## オフィス

### フロア拡張

- フロアを増やすごとに自動収益 **+30%**
- フロア拡張コスト: `80,000 × 5^(現フロア数 − 1)`

### オフィス設備

各種設備をアップグレードすることで自動収益にボーナスが加算される。

```
officeIncomeMultiplier = 1.0 + Σ(level × incomeBoostPerLevel)
```

---

## 都市（シティ）

- 空きプロットに建物を建設し、毎秒の収益を追加できる
- 建物はレベルアップが可能（最大レベルあり）
- 都市収益は `totalIncomePerSec` に直接加算される（倍率対象外）
- プロット数: 6（2列×3行）

---

## 放置システム

- アプリを閉じている間も社員が自動収益を稼ぎ続ける
- 放置上限: **8時間**（「夜勤延長」購入で **16時間**）
- 再起動時にオフライン収益をポップアップで通知

---

## セーブ仕様

- **自動セーブ**: 30秒ごと
- **強制セーブ**: バックグラウンド移行時
- **保存先**: UserDefaults

### 保存データ一覧

| データ | キー |
|--------|------|
| 所持金 | cg_money |
| 累計収益 | cg_totalEarned |
| 最終セーブ日時 | cg_lastSave |
| 社員状態 | cg_employees |
| 図鑑選択画像 | cg_selImg |
| 購入済みアップグレード | cg_upgrades |
| 時限ブースト | cg_boosts |
| オフィス設備 | cg_office |
| フロア数 | cg_floors |
| ガチャ天井カウント | cg_gachaPity |
| ガチャカード枚数 | cg_gachaCards |
| 都市建物 | cg_city |

> **Note**: あかり（id: 4）は削除済み。社員は4名（カレン・みさき・ゆき・りお）。

---

## 画像生成フロー

### 使用ツール

| ツール | 役割 |
|--------|------|
| Leonardo.ai（Web画面） | 画像生成（無料150トークン/日） |
| `import_images.py` | 背景除去 + xcassets 自動登録 |
| `Leonardo.md` | 生成設定の詳細 |

### Leonardo.ai 生成設定

| 項目 | 設定値 |
|------|--------|
| Model | Leonardo Anime XL |
| Style | anime illustration |
| Contrast | Low（1） |
| Image Dimensions | 2:3 |
| Size | 784 × 1176 |

**Negative Prompt:**
```
low quality, blurry, bad anatomy, extra fingers, deformed face, watermark, text,
ugly face, asymmetrical face, crooked face, distorted face, disfigured,
bad proportions, malformed, fused features, poorly drawn face, cloned face,
double face, mutation, mutated, gross proportions, missing eyes, extra eyes,
oversaturated, unnatural colors, color bleeding, neon colors, weird colors,
rainbow hair, multicolored hair, color noise, chromatic aberration
```

### キャラクター別プロンプト方針

| キャラ | 髪色 | 特徴 |
|--------|------|------|
| karen | ピンク（短め） | 明るい笑顔、営業部長 |
| misaki | 黒（ロング）+ 眼鏡 | クールな表情、開発部長 |
| yuki | シルバーホワイト | クールビューティ、管理部長 |
| rio | ピンク（ショート） | 元気な笑顔、マーケティング部長 |

**レアリティ別背景・衣装:**

| レアリティ | 衣装 | 背景 |
|-----------|------|------|
| N | カジュアル | 屋外・公園・街中・自然光 |
| R | ビジネス正装 | オフィス・窓からの陽光 |
| SR | ラグジュアリーファッション | レストラン・ルーフトップ・黄金時間 |
| SSR | 豪華なドレス・装飾品 | 夜景・ボケ照明・シネマティック |

### 画像追加手順

1. **Leonardo.ai Web画面**（[leonardo.ai](https://leonardo.ai)）で上記設定で生成
2. 画像をダウンロードし、命名規則に従ってリネーム
   - 例: `rio_3.jpg`, `akari_0.jpg`
3. 以下のフォルダに配置:
   ```
   swiftgame/clickgirl/downloaded_images/
   ```
4. Claude Code に「インポートして」と依頼
   → `import_images.py` が背景除去・xcassets登録を自動実行

### 注意事項

- Leonardo.ai の **APIは有料**（Web画面のみ無料）
- APIキーは `.env` に記載、**GitHubにアップしない**（`.gitignore` 設定済み）
- 1日150トークン、1枚あたり約10〜15トークン消費

---

## 収益倍率の適用順序

```
totalIncomePerSec
  = (Σ 社員収益/秒 × cardIncomeMultiplier)
      × incomeMultiplier      // ショップ永続 + 時限ブースト
      × officeIncomeMultiplier // オフィス設備
      × floorIncomeMultiplier  // フロア数
  + cityIncomePerSec           // 都市収益（倍率対象外）
```

---

## バグ修正履歴

### 2026-04-23

#### AdMob クラッシュ（EXC_BAD_ACCESS / EXC_I386_GPFLT）

**症状:** 起動直後に Thread 4/8 (concurrent) で `GADApplicationVerifyPublisherInitializedCorrectly` が NSException を投げてクラッシュ。

**原因:** `GENERATE_INFOPLIST_FILE = YES` + `INFOPLIST_KEY_GADApplicationIdentifier` のビルド設定では AdMob SDK v13 が `GADApplicationIdentifier` を Info.plist から読み取れない。

**調査過程:**
- ThreadSanitizer (TSan) が AdMob の GCD ディスパッチをラップして例外伝播を阻害していた → Xcode: Edit Scheme → Run → Diagnostics → Thread Sanitizer をオフ
- TSan 無効後も継続 → exception breakpoint + `po $arg1` で例外メッセージ確認
- メッセージ: `"The Google Mobile Ads SDK was initialized without an application ID."`

**修正内容:**
1. `AppDelegate.swift` を変更 — `MobileAds.shared.start()` を起動直後に即時呼び出し、ATTracking は 1 秒後に非同期で別途リクエスト
2. `ClickGirl/Info.plist` を手動作成 — `GADApplicationIdentifier` を直接記載（テスト ID: `ca-app-pub-3940256099942544~1458002511`）。`CFBundleIdentifier` 等の必須 bundle キーも含む
3. `project.pbxproj` の ClickGirl ターゲット（Debug/Release）を変更:
   - `GENERATE_INFOPLIST_FILE = NO`
   - `INFOPLIST_FILE = ClickGirl/Info.plist`
   - `INFOPLIST_KEY_*` 系キーを削除
4. `PBXFileSystemSynchronizedBuildFileExceptionSet` を pbxproj に追加し `Info.plist` をリソースコピーから除外（"Multiple commands produce Info.plist" エラーの回避）

**本番リリース時の注意:** `Info.plist` の `GADApplicationIdentifier` を実際の AdMob App ID に変更すること。

---

#### CharSelectScene キャラ画像の歪み

**症状:** キャラ選択画面のカード内キャラ画像が正方形に潰れて歪んで表示される。

**原因:** `SKTexture(imageNamed:)` はシーン遷移直後に `size()` が `(1, 1)` を返すことがある。これによりアスペクト比が 1:1 と誤認識され、縦長画像が正方形に引き延ばされる。また、高さ上限クランプ時に幅を調整しないため比率が崩れていた。

**修正内容（`CharSelectScene.swift` `makeCard` 関数）:**
- `UIImage(named:)` でアスペクト比を正確に取得（`SKTexture.size()` の代わり）
- 高さが上限（`h * 0.62`）を超えた場合、幅も縮小してアスペクト比を保持（aspectFit）

---

## 共通UIデザイン原則

全シーンで統一されているデザインパターン:

| 要素 | 仕様 |
|------|------|
| 背景色 | `(0.04, 0.04, 0.12)` ダーク紺 |
| ゴールドアクセント | `(1.0, 0.85, 0.2)` |
| グリーン収益色 | `(0.38, 0.98, 0.52)` |
| TopBarアクセントライン | height=1.5pt、金色、下端に配置 |
| アフォーダブルグロー | 青系パルスリング（0.75〜1.0s周期） |
| 収益ピル | 緑背景 + 緑枠、"+¥N/s" 表示 |
| エラートースト | 画面下部スライドアップ、2秒表示 |
| 建設完了バナー | 中央ポップイン + 金パーティクル |
